#include <stdio.h>                 // Include the standard input-output header file

int main() {                       // printf() comes from this stdio.h
    printf("Hello, World!\n");     // int means the function returns an integer value
    return 0;                      // if all is well, return the value 0 to the operating system and ends main() function
}










